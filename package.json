{
  "name": "stylesheets",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "coverage:erase": "coverage erase",
    "coverage:run": "coverage run manage.py test",
    "coverage:report": "coverage report",
    "test": "npm run coverage:erase && npm run coverage:run && npm run coverage:report",
    "python-setup": "pip install pipenv && pipenv install",
    "setup": "npm run python-setup && npm install",
    "build": "gulp && pipenv run python manage.py collectstatic --noinput",
    "start": "pipenv run python manage.py runserver"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bourbon": "^6.0.0",
    "getbuffalowater-bourbon-neat": "^1.8.0",
    "gulp-sass": "^4.0.2",
    "node-sass": "^4.12.0",
    "normalize.css": "^8.0.1",
    "npm": "^6.12.0"
  },
  "devDependencies": {
    "gulp": "^4.0.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/CodeForBuffalo/affordable_water"
  }
}
