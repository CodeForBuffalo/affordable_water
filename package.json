{
  "name": "getbuffalowater",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "python-setup": "pip install pipenv && pipenv install",
    "setup": "npm run python-setup && npm install",
    "gulp": "gulp",
    "collectstatic": "python manage.py collectstatic --noinput",
    "compress": "python manage.py compress",
    "start": "pipenv run python manage.py runserver",
    "heroku-prebuild": "npm install",
    "heroku-postbuild": "npm run gulp && npm run collectstatic && compress"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bourbon": "^6.0.0",
    "getbuffalowater-bourbon-neat": "^1.8.0",
    "gulp-sass": "^4.0.2",
    "node-sass": "^4.13.0",
    "normalize.css": "^8.0.1"
  },
  "devDependencies": {
    "gulp": "^4.0.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/CodeForBuffalo/affordable_water"
  }
}
